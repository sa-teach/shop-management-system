# Hike Shop Management System

Система управления магазином туристического снаряжения на Windows Forms.

##  Описание 

Это учебный проект на C# Windows Forms для управления магазином. Система позволяет управлять пользователями, товарами, клиентами и продажами.

##  Структура 

### Точка входа приложения

**Program.cs** - главный файл приложения
- Запускает SplashForm при старте
- Настраивает визуальные стили Windows Forms

### Формы приложения (последовательность запуска)

1. **SplashForm.cs** - экран загрузки
   - Показывает прогресс-бар загрузки
   - После завершения открывает LoginForm
   - Содержит таймер для анимации загрузки

2. **LoginForm.cs** - форма входа
   - Поля для ввода логина и пароля
   - После успешного входа должна открывать MainForm

3. **MainForm.cs** - главная форма приложения
   - Содержит меню навигации
   - Кнопки для перехода к разным модулям:
     - Dashboard (главная панель)
     - Customers (клиенты)
     - Users (пользователи)
     - Products (товары)
     - Cash (касса)
   - Кнопка выхода из системы

4. **UserForm.cs** - форма управления пользователями
   - Отображает список пользователей в DataGridView
   - Поиск пользователей в реальном времени
   - Кнопка добавления нового пользователя
   - Кнопки редактирования и удаления в таблице
   - Использует параметризованные SQL-запросы для безопасности

5. **UserModule.cs** - форма добавления/редактирования пользователя
   - Поля для ввода данных пользователя:
     - Имя (Name)
     - Адрес (Address)
     - Телефон (Phone)
     - Роль (Role) - Admin или Employee
     - Дата рождения (Date of Birth)
     - Пароль (Password) - только для Admin
   - Валидация данных (проверка возраста, обязательные поля)
   - Сохранение пользователя в базу данных

### Вспомогательные классы

**DbConnect.cs** - класс для подключения к базе данных
- Метод `Connection()` возвращает строку подключения
- Использует относительный путь к базе данных
- База данных находится в папке `bin\Debug\dbHikeShop.mdf`

##  База данных

База данных: **dbHikeShop.mdf** (SQL Server LocalDB)

Таблица **tbUser** содержит:
- `id` - идентификатор пользователя
- `name` - имя пользователя
- `address` - адрес
- `phone` - телефон
- `role` - роль (Admin/Employee)
- `dob` - дата рождения
- `password` - пароль

##  Зависимости

- **.NET Framework 4.8**
- **Guna.UI2.WinForms** (версия 2.0.4.5) - библиотека для современных UI компонентов
- **SQL Server LocalDB** - для работы с базой данных

##  Установка и запуск

### Требования
- Visual Studio 2019 или новее
- .NET Framework 4.8
- SQL Server Express LocalDB

### Шаги установки

1. Клонируйте репозиторий или скачайте проект
2. Откройте файл `Shop Management System.sln` в Visual Studio
3. Убедитесь, что база данных `dbHikeShop.mdf` находится в папке `bin\Debug`
4. Восстановите NuGet пакеты (правая кнопка на Solution → Restore NuGet Packages)
5. Нажмите F5 для запуска проекта

### Настройка базы данных

Если база данных не подключена:
1. Скопируйте файлы `dbHikeShop.mdf` и `dbHikeShop_log.ldf` в папку `bin\Debug`
2. Убедитесь, что SQL Server LocalDB установлен
3. При необходимости измените строку подключения в `DbConnect.cs`

## 🔐 Данные для авторизации

### Создание тестового пользователя

Перед первым входом в систему необходимо создать пользователя через форму управления пользователями:

1. Запустите приложение
2. После SplashForm откроется LoginForm (форма входа)
3. **Примечание:** В текущей версии форма входа еще не реализована полностью. Для доступа к системе:
   - Измените точку входа в `Program.cs` временно на `Application.Run(new UserForm());`
   - Или реализуйте логику авторизации в `LoginForm.cs`

4. В UserForm нажмите кнопку "+" для добавления пользователя
5. Заполните форму:
   - **Имя:** `admin`
   - **Адрес:** `Администратор`
   - **Телефон:** `+1234567890`
   - **Роль:** `Admin`
   - **Дата рождения:** любая дата (пользователь должен быть старше 18 лет)
   - **Пароль:** `admin123`

### Тестовые данные для входа

После создания пользователя используйте следующие данные:

**Для администратора:**
- **Логин:** `admin` (или имя пользователя из базы данных)
- **Пароль:** `admin123` (или пароль, указанный при создании)

**Для сотрудника:**
- **Логин:** имя пользователя с ролью `Employee`
- **Пароль:** пароль не требуется для сотрудников

### Важно

- В текущей версии авторизация через LoginForm еще не реализована
- Для тестирования функций управления пользователями можно временно запускать UserForm напрямую
- Пароль хранится в базе данных в открытом виде (для продакшена требуется хеширование)

##  Как работает приложение

### Последовательность запуска:

```
Program.cs (Main)
    ↓
SplashForm (экран загрузки)
    ↓
LoginForm (форма входа)
    ↓
MainForm (главное меню)
    ↓
UserForm (управление пользователями)
    ↓
UserModule (добавление/редактирование)
```

### Основные функции

#### Поиск пользователей
- Введите текст в поле поиска в UserForm
- Список автоматически фильтруется по имени, адресу, телефону, дате рождения или роли
- Используется параметризованный SQL-запрос для защиты от SQL-инъекций

#### Добавление пользователя
1. Нажмите кнопку "+" в UserForm
2. Заполните все обязательные поля в UserModule
3. Выберите роль (Admin или Employee)
4. Если выбрана роль Employee, поле пароля скрывается
5. Нажмите "Save" для сохранения

#### Удаление пользователя
1. Нажмите иконку удаления в строке пользователя
2. Подтвердите удаление
3. Пользователь будет удален из базы данных

#### Редактирование пользователя
- Функция редактирования подготовлена, но требует доработки
- При нажатии на иконку редактирования показывается сообщение

##  Безопасность

- Используются параметризованные SQL-запросы для защиты от SQL-инъекций
- Валидация данных перед сохранением в базу
- Проверка возраста пользователя (не менее 18 лет)

## Важные замечания

### Что в этом проекте:

1. **Windows Forms** - основы создания десктопных приложений
2. **ADO.NET** - работа с базами данных через SqlConnection, SqlCommand
3. **DataGridView** - отображение данных в таблице
4. **Параметризованные запросы** - защита от SQL-инъекций
5. **Обработка событий** - события кнопок, текстовых полей, DataGridView
6. **Валидация данных** - проверка введенных данных перед сохранением
7. **Относительные пути** - использование Application.StartupPath для путей к файлам

### Частые ошибки и решения:

**Ошибка подключения к базе данных**
- Проверьте, что файл `dbHikeShop.mdf` находится в `bin\Debug`
- Убедитесь, что SQL Server LocalDB установлен
- Проверьте строку подключения в `DbConnect.cs`

**Ошибка "Guna.UI2 не найден"**
- Восстановите NuGet пакеты: правой кнопкой на Solution → Restore NuGet Packages
- Или установите вручную: `Install-Package Guna.UI2.WinForms`

**Приложение не запускается**
- Проверьте, что в `Program.cs` запускается `SplashForm`
- Убедитесь, что все формы правильно инициализированы


## Учебный проект для демонстрации студентам.

**Примечание:** Это учебный проект. Для продакшена требуется дополнительная доработка безопасности, обработки ошибок и функциональности.
